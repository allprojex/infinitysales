# URL of your externally-hosted api-server (artifacts/api-server in your monorepo).
# Defaults to the page origin if unset. Set this to wherever you've deployed Express
# (Render, Fly, Railway, VPS, etc.) and make sure CORS on the server allows this
# Lovable preview/published domain.
VITE_API_BASE_URL=http://localhost:3000
